# CLAUDE.md — GRD Dashboard төслийн контекст

Говь Ресурс Девелопмент ХХК (нүүрс угаах үйлдвэр)-ийн өдөр тутмын үйл
ажиллагааны тайлангийн web dashboard. Claude-тай хамтран хөгжүүлж ирсэн;
энэ файл нь шинэ session/PC/Claude Code-д бүрэн контекст дамжуулна.

## Систем юу хийдэг вэ
- Ажилтан нэвтрээд өөрийн эрхтэй тайлангийн form бөглөнө (7 модуль:
  Үйлдвэрлэл/Лаб, Тээвэр, Түлш, Техник, Кемп/хүн хүч, ХАБЭА, Асуудал).
- Бүх нэвтэрсэн хэрэглэгч dashboard.html дээр өдрийн болон сарын нэгтгэл харна.
- Ганц login (index.html), role-оор чиглүүлнэ: worker → report.html,
  admin → dashboard.html. admin.html = хэрэглэгчийн удирдлага (зөвхөн admin).

## Технологи ба deploy
- Cloudflare Pages + Pages Functions + D1 SQLite (binding: DB → govi_dashboard).
- Frontend: vanilla JS (framework байхгүй), нэг app.js, нэг style.css.
  График бүгд гар бичмэл inline SVG/HTML (сан ашигладаггүй).
- **Deploy: terminal-аас `.\deploy.ps1`** — html/js/css/png БОЛОН
  `transport-icons\` хавтсыг dist-д хуулж
  `npx wrangler pages deploy dist --project-name report --branch main`.
  Project нэр нь **report** (домэйн report-d3e.pages.dev). GitHub автомат
  deploy УНТРААСАН — GitHub push нь сайтад нөлөөлөхгүй, wrangler л deploy хийнэ.
- `.\deploy_preview.ps1` — production-д хүрэхгүйгээр preview branch руу гаргана.
  Батлуулах шаардлагатай засварыг эхлээд үүгээр, эсвэл өөрөө биеэ даасан
  `_preview_dashboard.html` файлаар харуулна.
- Хоёр скрипт хоёулаа `$LASTEXITCODE`-ыг шалгадаг: wrangler унавал ногоон
  «Deploy OK» гэж ХУДЛАА бичихгүй, улаанаар бүтэлгүйтсэнийг мэдэгдэнэ.
  (Өмнө нь энэ шалгалт байхгүйгээс deploy болоогүй мөртлөө OK гэж хуурсан.)
- **`transport-icons\` доторх 3 SVG заавал deploy-д орно** — тээврийн картын
  legend тэдгээрийг `<img src="transport-icons/...">`-ээр дууддаг.
- dist-д schema.sql, CLAUDE.md, test_backend.cjs зэрэг дотоод файл ХЭЗЭЭ Ч
  оруулахгүй (static asset болж нийтэд ил гардаг байсныг зассан түүхтэй).
- wrangler.toml-ийг repo/хавтсанд БАЙЛГАХГҮЙ.
- schema.sql-ийг production D1 дээр ДАХИН АЖИЛЛУУЛАХГҮЙ (DROP TABLE-тай).
  Өөрчлөлтөд migration_*.sql бичиж D1 Console-оор гараар оруулна.
- Тест: test_backend.cjs (D1-ийг node:sqlite-ээр дуурайлгадаг).
- Syntax шалгалт: `node --check app.js` ба functions файл. CSS-ийг {} тэнцүү
  эсэхээр шалгадаг заншилтай.

## Хэрэглэгч, эрх (2026-07 сүүлч)
- Нэвтрэлт: нэвтрэх нэр + **нууц үг** (4-20 тэмдэгт; «PIN» нэршил халагдсан).
- Хэлтэс бүрт хэд хэдэн нэртэй ажилтан бүртгэж болно (жишээ: Тээвэр Ууганбаяр);
  тайланг хэн илгээснийг самбарт нэрээр харуулна.
- admin.html: хэрэглэгч нэмэх (эрх нь тайлангийн төрлийн chip-ээр),
  нэвтрэх нэр + ажилтны нэр солих, нууц үг солих, идэвхгүй болгох/сэргээх.
- Backend admin endpoints: users, users/setpin, users/rename, users/create,
  users/toggle. Нууц үгийг audit_logs-д хэзээ ч бичихгүй.
- АНХААР: хуучин schema.sql нийтэд ил байсан тул анхны нууц үгүүд задарсан
  гэж үзнэ — бүгдийг сольсон/солих ёстой.

## Бизнесийн ойлголт — ТЭЭВРИЙН 3 ТӨРӨЛ (чухал!)
1. **Шлам** — Energy Resource(ER)-ээс үйлдвэр лүү ТАТАЖ авчирдаг түүхий эд.
2. **Бүтээгдэхүүн тээвэр** — дотроо: **богино** (хашаан доторх, purpose='short')
   + **урт** (ER руу явуулдаг, purpose='product').
3. **Хаягдал** — баяжуулаад үлдсэнийг гаргадаг тээвэр (purpose='waste').
- Төрөл бүр өөрийн машинтай. Өмчлөл: own / rental_product / rental_sludge.
- **Пүүний жин (weighbridge_net_ton) = ER-ээс ирсэн нийт шламны жин** —
  шламын албан ёсны хэмжилт.
- Дэлгэц дээр богино рейс **Бүтээгдэхүүнд** тоологдоно (Хаягдалд БИШ — 2026-07
  сүүлчээр зассан). Өгөгдлийн талбар нь түүхэн шалтгаанаар short_waste_* нэртэй
  хэвээр.

## Сарын төлөвлөгөө (admin оруулна)
Метрикүүд (PLAN_METRICS): Бүтээгдэхүүн үйлдвэрлэлт /тн/ (production_ton),
Хаягдал /тн/ (sludge_ton talbar!), Шлам /рейс + машины тоо/ (sludge_trips,
sludge_vehicles), Бүтээгдэхүүн /рейс + машины тоо/ (product_transport_trips,
product_transport_vehicles). plan_json чөлөөт JSON тул шинэ түлхүүр нэмэхэд
migration хэрэггүй.

### НЭЭЛТТЭЙ АСУУЛТУУД (захиалагчаас хариу хүлээгдэж буй)
1. «Хаягдал /тн/» төлөвлөгөөний бодит гүйцэтгэл одоо ШЛАМЫН тонноос (sludge_ton)
   тоологдож байгаа. ER шламын татан авалт мөн бол пүүний жингээс тооцох уу?
   Нэр нь «Хаягдал» мөн үү? (Захиалагч өөрөө «Шлам олборлолт»-ыг «Хаягдал»
   болгож нэрлэсэн.)
2. Бүтээгдэхүүний рейсийн төлөвлөгөөнд богино рейс орох уу (одоо зөвхөн
   product_transport_trips = урт тоологддог)?
3. Богино/урт задаргааг самбарт хэр дэлгэрэнгүй харуулах вэ?

## Дизайны систем (style.css)
- Уур амьсгал: цэвэр минимал SaaS (Workload template-ээс санаа авсан).
  Glassmorphism, амбиент толбо, blur ХЭРЭГЛЭХГҮЙ (V14-өөс хойш).
- Дэвсгэр linear-gradient(#F5F6F5→#EEF0EF); панел/карт цагаан, radius 18-22px,
  зөөлөн диффуз сүүдэр; доторх блокууд #FAFBFA.
- Брэнд улаан #BC2029 (logo.png-ээс) — hero, featured карт, зарлагын график.
  Ногоон #34C759 эерэг утга. Статус: ногоон/шар/улаан + заавал текст тайлбар.
- Шошго 10.5px UPPERCASE #98A0A6; гол тоо 34-36px w800; бүх тоо tabular-nums;
  товч бүгд pill.
- **Фонт: Inter (ганц).** 2026-08-13-нд зассан. Өмнө нь `-apple-system` эхэлж
  байсан тул Mac/iPhone дээр SF Pro, Windows дээр Inter гарч ТӨХӨӨРӨМЖ БҮР
  ӨӨР харагддаг байв. Одоо `--font: 'Inter', system-ui, ...`.
  Inter-ийг `style.css`-ийн `@import`-оор БИШ, HTML бүрийн `<head>`-д
  `preconnect` + `link`-ээр татна (@import нь 3 дараалсан алхам үүсгэдэг тул
  удаан сүлжээнд текст үсэрдэг). **Шинэ HTML нэмбэл тэр 3 мөрийг хуулж тавь.**
  Татац: **`wght@400..800`** — хувьсах (variable) фонт, нэг файл.
  2026-08-13: 800 жинг 600/650 болгох санал preview-ээр үзүүлсэн боловч
  **захиалагч 800-г сонгосон** («800 дээр харагдаж байна»). Тиймээс `style.css`-д
  `font-weight:800` 28 газар ХЭВЭЭР үлдсэн.
  АНХААР: жинг татацаас хасвал хөтөч 700-г зохиомлоор сунгадаг (synthetic bold)
  бөгөөд энэ нь жинхэнэ 800-аас МУУ харагддаг. Тиймээс CSS-д ашиглаж буй жин
  бүр татацын мужид багтаж байх ёстой. Одоо 400..800 муж бүгдийг хамарна.
  SF Pro-г вэбэд түгээх боломжгүй — Apple-ийн лиценз хориглодог.
- Текст хэзээ ч таслагдахгүй: fmtShort (14185→14k), урт нэр «…»/ellipsis.
  Нэг тоог хоёр газар давхардуулахгүй.

## ХУУДАСНУУД (2026-08 сүүлч, V19)
1. **dashboard.html — Хяналтын самбар**: дээд мөр [hero (огноо, ‹ ›) | цаг агаарын карт]
   → осол гэмтэлгүй хоногийн зурвас → Тайлангийн ирц (7 модуль, хэн хэдэн цагт)
   → Гол үзүүлэлтүүд (KpiCards). ЗӨВХӨН ӨДРИЙН харагдац — хугацааны сонголтгүй.
2. **meeting.html — Хурал**: hero (хурлын огноо + segmented control «7 хоног /
   Сар / 3 сар») → Гүйцэтгэл (САМБАРТАЙ ИЖИЛ KpiCards, хурлын өмнөх хугацааны
   нэгтгэлээр) → Асуудлууд → Өмнөх хурлын даалгавар → Энэ хурлын даалгавар
   (админ засна) → Тэмдэглэл → Хугацааны бүх даалгавар.
3. **report.html — Тайлан оруулах**: «Миний хийх ажил» (даалгаврын төлөв +
   тайлбар) → тайлангийн form → машины бүртгэл.
4. **admin.html — Хэрэглэгчид**.

### Гол JS модулиуд (app.js доторх, дэлхийн хамрах хүрээнд)
- `Aggregate.byType(reports)` — олон өдрийн тайланг НЭГ өдрийн хэлбэрт хувиргана.
  Дүрэм: тонн/литр/рейс/зөрчил = SUM; **машин, хүн хүчний тоо = AVG** (нэмбэл
  утгагүй болно); түлшний эхний үлдэгдэл = FIRST, эцсийн = LAST; тоолуур/лаб/
  цаг агаар = AVG. Машины мөрүүд машин бүрээр нэгтгэгдэнэ.
- `KpiCards.render(box, curMap, prevMap, {plan, date, span})` — самбар ба хурлын
  хуудсанд ИЖИЛ картуудыг зурна. span > 1 үед gauge-ийн зорилт = өдрийн норм ×
  хоног, trend chip «өмнөх хугацаанаас» гэж бичигдэнэ.
- `RangeReport` — хугацааны туслах (addDays, daysBetween) + бүрэн тайлан зурах
  `render(box, from, to)` (одоо шууд ашиглагдахгүй, туслахууд нь хэрэгтэй).

## Хуучин Dashboard-ын бүтэц (лавлагаа, V17)
1. Улаан hero: сонгосон өдрийн он сар өдөр ТОМООР + «Тайлангийн нэгтгэл» +
   огноо сонгогч. Topbar: Тайлан · Хэрэглэгчид(admin) · нэр · Гарах.
2. Тайлангийн ирц: 7 модуль, хэн хэдэн цагт илгээсэн, 0/7 ring (нэмэлт
   текстгүй). Модуль дээр дарвал дэлгэрэнгүй.
3. Өдрийн гол үзүүлэлтүүд (2026-08-05-нд шинэчилсэн):
   - **Бүтээгдэхүүн** — featured улаан карт, хурдны хэмжүүр (speedometer)
     маягийн gauge. Нум улаанаас ногоон руу шилжих gradient-тэй, 13 зураас,
     үзүүрт нь цагаан цагираг. Дүүргэлт = өдрийн норм (сарын төлөвлөгөө ÷
     тухайн сарын хоног), 100%-д дүүрч зогсоно. Нумын доор зүүн талд
     «Өдрийн зорилт · X т», баруун талд «Биелэлт N%».
   - **Тээвэр** — donut (216px, зузаан 28, завсар 5). Шлам #3B2FE0 /
     Хаягдал #9BA3A9 / Бүтээгдэхүүн #FF9500, богино рейс Бүтээгдэхүүнд.
     Голд нь зөвхөн нийт тоо + «тн», фонт нь оронгийн тоогоор автоматаар
     багасч нүхэндээ багтдаг (44px→22px). Legend-д SVG дүрс бүхий мөрүүд.
   - **Түлш** — график БАЙХГҮЙ, зөвхөн Орлого / Зарлага / Үлдэгдэл гэсэн
     3 мөр тоо (`miniStyle:'stats'`). Шалтгаан: түлшинд «дундаж» гэж
     харьцуулах суурь өгөгдөл байхгүй тул бар зурах нь утгагүй.
   - Техник, Хүн хүч — хуваарилалтын зурвас + legend; ХАБ — chip-үүд.
   - **Асуудал** — бүтэн мөр эзлэх НЭГТГЭСЭН карт (доор тусад нь тайлбарласан).
   - Бүгд ▲▼ trend chip-тэй.
4. **Сарын төлөвлөгөөний биелэлт** (тусдаа хэсэг): legend (Түрүүлсэн/Ойролцоо/
   Хоцорсон) + бараан «Сарын явц N%» pill + 4 карт: % pill, том тоо/төлөвлөгөө,
   pace шугамтай bar, «Өнөөдрийн зорилт · X» + статус текст («Түрүүлсэн +7%»),
   өдөр тутмын мини багана, «Ажиллаж буй: N машин» note. Үнэлгээ = биелэлтийн
   % — сарын явцын % зөрүү (≥0 ногоон, ≥−20 шар, бусад улаан).
   «Төлөвлөгөө засах» (admin) энд.
5. **Сарын нэгтгэл**: 3 сэдэвчилсэн блок — Түлш (орлого ногоон/зарлага улаан
   өдрийн багана), Тээвэр (нийт тн + Пүүний жин), ХАБЭА ба Асуудал (chip +
   багана). Төлөвлөгөөтэй тоонууд энд ДАВХАРДАХГҮЙ.
6. **Машин тус бүрийн үр бүтээмж**: зүүн — л/тонн эрэмбийн bar (шилдэг 10,
   өмчлөлийн өнгөөр: own #3D4A63, rental_product #D97A16, rental_sludge
   #1F8FA3), баруун — машин бүр avatar-тай карт (★ Шилдэг ногоон хүрээ,
   хамгийн муу улаан тоо), доор бүрэн хүснэгт (машин/түлш/рейс/тонн/л·тонн).

## ХУРЛЫН МОДУЛЬ (2026-08 нэмсэн)
- D1 хүснэгт: `meetings` (meeting_date UNIQUE, notes) + `meeting_tasks`
  (task_text, assignee_id, due_date, status: open/done/postponed, worker_note).
  **`migration_meetings.sql`-ийг D1 Console дээр НЭГ УДАА ажиллуулна** — эс бөгөөс
  хурлын хэсэг ажиллахгүй.
- Endpoints: meetings, meeting, meeting/save (admin), tasks/mine, tasks/status,
  range (интервалын тайлан + тухайн хугацаанд өгсөн даалгавар).
- Даалгаврыг **хурлын огноогоор** шүүнэ (өгсөн огноо), эцсийн хугацаагаар биш.
- Ажилтан өөрийн даалгаврын төлөв + тайлбарыг report.html-ээс сольдог.

## ХҮЛЭЭГДЭЖ БУЙ АЖЛУУД (2026-08-13 байдлаар)
1. **ХАБЭА-гийн шинэ form** — макет батлагдахыг хүлээж байна. Дараа нь
   «осолгүй ажилласан хүн/цаг» тоо аюулгүй ажиллагааны зурваст нэмэгдэнэ.
2. **Excel-ийн 7-р сарын өгөгдлийн импорт** — бодит түүхэн дата оруулах.
3. `migration_meetings.sql`-ийг D1 Console дээр НЭГ УДАА ажиллуулах (хурлын
   модуль үүнгүйгээр ажиллахгүй).
4. Тээврийн бүтцийн 3 нээлттэй асуулт (дээрх «Сарын төлөвлөгөө» хэсэгт).
5. Нууц үгийн hash (одоо plain text) + хуучин ил байсан нууц үгүүдийг солих.

## ТҮР ХАСАГДСАН — дараа нэгтгэх (dashboard.html-д коммент үлдээсэн)
- Сарын төлөвлөгөөний биелэлт (#planArea) — pl-card бүтэц кодод бэлэн.
- Сарын нэгтгэл (#monthlyCards) — сэдэвчилсэн блокууд.
- Машин тус бүрийн үр бүтээмж (#monthlyMachines) — л/тонн эрэмбэ + машины карт.
Эдгээрийн код app.js-д ХЭВЭЭР байгаа, зөвхөн HTML-ээс салгасан.
**2026-08-12: захиалагч «хассан хэвээр үлдээ, би шинээр хийхээр төлөвлөж байна» гэж
баталсан. ЗӨВШӨӨРӨЛГҮЙГЭЭР буцааж оруулж болохгүй.** Эргүүлж оруулах бол dashboard.html-д
#planArea, #monthlyCards, #monthlyMachines гэсэн гурван id-тай элемент нэмэхэд хангалттай.

## Файлын бүтэц
- index.html / dashboard.html / report.html / admin.html / meeting.html
- app.js — CONFIG (тайлангийн төрөл, form, KPI) / SESSION / API / UI
  (fmt, fmtShort, gaugeHtml, donutHtml, miniViz, waveChartHtml...) /
  PageLogin / PageDashboard / PageReport / PageAdmin
- style.css — дизайны систем (V14 Workload, V15 KPI, V16 сэдэвчилсэн,
  V17 план+үр бүтээмж гэсэн давхаргуудтай)
- functions/api/[[path]].js — бүх backend
- **transport-icons/** — sludge-bed.svg, waste-pile.svg, product-coal.svg
  (тээврийн картын legend-ийн дүрс; deploy-д ЗААВАЛ орно)
- schema.sql, migration_*.sql, test_backend.cjs, logo.png
- deploy.ps1 (production), deploy_preview.ps1 (preview), git_save.ps1 (GitHub)

## ТОЛГОЙ ХЭСЭГ V19.5 — ОГНОО + ХАБЭА + ЦАГ АГААР (2026-08-14 шинэчилсэн)

Самбарын дээд мөр = ГУРВАН карт: **огнооны улаан карт** (өргөн) → **ХАБЭА-гийн
ногоон босоо карт** → **цаг агаарын улаан ДӨРВӨЛЖИН карт**. Захиалагчийн өөрийн
явуулсан `grd-dashboard-header.html` (Claude artifact) загварыг preview-ээр
олон удаа засварлаж батлуулсны дараа орсон. Хуучин 920×320 SVG карт болон
бүтэн өргөний ногоон зурвас ХАСАГДСАН (`.wxc-*` CSS устгагдсан).

**Цаг агаарын карт** (`.site-card`, aspect-ratio 1:1 тул hero-гийн өндрийг
дагана — гараар px тааруулах хэрэггүй):
- «Үйлдвэрийн талбай» chip (ногоон pulse цэгтэй) дээд талд.
- Нар/сар SVG (viewBox `21 6 66 59` — тайрсан тул зураг хүрээгээ дүүргэдэг)
  ба хэм зэрэгцээ. **Нар↔сар, «ӨДӨР 26°C»↔«ШӨНӨ 13°C» ЯГ ЗЭРЭГ ээлжилнэ** —
  8 секундын мөчлөг, -4с delay (захиалагч «4 секунд» гэж хүссэн = тал бүр 4с).
  Шошго нь тооны ДООР (column-reverse).
- Чийгшил / салхи — 20px ДӨРВӨЛЖИН icon-той хоёр мөр, шошго/утга нэг мөрөнд.
- `renderWeather()` хэвээр: `#temperature #night-temperature #humidity
  #wind-speed` id-д утга суулгана. НЭГ засвар: night-temperature-д '°'
  залгахаа больсон — нэгжийг markup барина. ХАБЭА тайлангүй өдөр карт нуугдана.

**ХАБЭА карт** (`renderSafety()` — markup ӨӨРЧЛӨГДӨӨГҮЙ, зөвхөн CSS):
`.top-row .safety-bar`-ыг grid + `display:contents`-ээр босоо карт болгосон.
Бүтэц: шошго (нэг мөр, голд) → icon 64px + «6» 64px зэрэгцээ голд → зураас →
«Сүүлийн тохиолдол · огноо» (нэг мөр, голд). Өнөөдөр тохиолдолтой бол улбар шар
(`.warn`). Markup-ийн дараалал: sb-ico, div(sb-l+sb-v), sb-sub — CSS order-оор
байрлуулсан тул app.js-ийн renderSafety-г ХӨНДӨХГҮЙ.

АНХААР: `.card` класс KPI картуудад бас ашиглагддаг тул толгойн улаан картын
бүх дүрэм `.top-row .card` гэж хамрагдсан — хамрах хүрээг алдвал бүх KPI карт
улаан болно (preview дээр гарч байсан алдаа).

## 2026-08-12-нд ЗАССАН 3 АЛДАА (дахин гаргахгүй байх)

1. **`SEV_LABEL is not defined`** — асуудлын жагсаалтыг зурах код `PageDashboard`-аас
   `KpiCards` руу гарсан ч хувьсагч нь хуучин байрандаа үлдсэн. Ноцтой байдалтай асуудал
   тухайн хугацаанд байвал KPI карт БҮХЭЛДЭЭ уначихдаг байв.
   → Refactor хийсний дараа хувьсагч аль scope-д үлдсэнийг ЗААВАЛ шалга.
2. **`Cannot access 'VEHICLES' before initialization`** — `PageReport` нь `selectReport()`-ыг
   `let VEHICLES` зарлахаас ӨМНӨ дууддаг байсан. Зөвхөн teever/tulsh (цорын ганц эрх нь
   Тээвэр/Түлш) нэвтрэлтэд гардаг тул **админаар тестлэхэд харагддаггүй** — 2026-07-03-наас
   хойш live дээр байсан. → Шинэ хуудсыг ажилтны нэвтрэлтээр ЗААВАЛ тестэл.
3. **Ирцэд цаг/нэр алга** — самбар `/api/daily`-ээс `/api/range` руу шилжсэн ч `handleRange`
   нь `submitted_by_name`/`updated_at` буцаадаггүй байв. → LEFT JOIN users нэмсэн.

## Мэдэгдэж буй асуудлууд
- Машины бүртгэлд `waste` ба `short` зориулалттай машин НЭГ Ч БАЙХГҮЙ → самбарын донат дээр
  «Хаягдал» үргэлж 0 харагдана. Захиалагчаар машинуудыг бүртгүүлэх шаардлагатай.
- `Aggregate.byType` нь дүрэм тодорхойлоогүй талбарыг (issue_severity, note, equipment_note)
  хаядаг тул модулиудын асуудлын ноцтой байдал самбарт хэзээ ч гардаггүй.
- README_MN.md дотор анхны PIN-үүдийн хүснэгт байгаа бөгөөд repo нь public.
- Тестийн үеийн зарим өгөгдөл бодит бус (нэг машинд 25,077 тн → л/тонн 0.01,
  шламын рейс хэт өндөр г.м.) — бодит хэрэглээ эхэлмэгц арилна.
- Нууц үг plain text хадгалагддаг — дараагийн шатны ажил (hash).
- **`DASHBOARD_REQUIREMENTS.md` ХУУЧИРСАН — болгоомжтой ханд.** Тэр файлыг
  татгалзсан V20 дизайн хүчинтэй байх үед бичсэн тул одоогийн байдалтай
  зөрчилддөг. Үнэн эх сурвалж нь ЭНЭ файл (CLAUDE.md) ба бодит код.
- Mockup файлууд (`_mockup_*.html`), урьдчилан харах файлууд
  (`_preview_*.html`, `_gauge_demo.html`, `_donut_fit_test.html`, мөн
  `_build_*_preview.cjs` үүсгэгчид) нь .gitignore-д орсон — GitHub руу
  ордоггүй, зөвхөн ажлын компьютер дээр байна.

## 2026-08-13: ТӨВИЙГ САХИСАН ПАЛИТРЫН САНАЛ — ТАТГАЛЗСАН

Модулийн 6 өнгийг (Apple системийн палитр) устгаж нүүрс/тоос/ганц брэнд улаан
болгох, gauge-ийн улаан→ногоон gradient-ийг нэг өнгө болгох, тээврийн зурсан
icon-ыг өнгөний дөрвөлжингөөр солих саналыг `_preview_palette.html`-ээр
үзүүлсэн. Захиалагч **«онцгүй юм байна», «хуучнаар л байя»** гэж шийдсэн.
**Дахин санал болгохгүй.** Одоогийн өнгө (`--c-production:#0A84FF` гэх мэт)
ХЭВЭЭР.

Шударга тэмдэглэл — preview-д ХОЁР алдаа байсан тул үнэлгээ бүрэн шударга
байгаагүй байж магадгүй:
1. Картын icon-ы дэвсгэрийг `.asset-tag` гэж буруу тааруулсан. Жинхэнэ класс
   нь **`.mchip`** бөгөөд өнгө нь `style="background:${type.color}"` гэж
   **inline** тавигддаг (app.js мөр 701/747/1442). Үр дүнд нь дүрс дэвсгэртэйгээ
   ижил өнгө болж алга болсон — хар дөрвөлжингүүд харагдсан.
2. Төвийг сахисан өнгийг хэт бараан (#33383B) авсан тул «намдуу» биш
   «хатуу» харагдсан. Улаан hero + хар/улаан донат зэрэгцэн хэт түрэмгий болсон.

Дахин оролдох тохиолдолд мэдэх ёстой техникийн зүйлс:
- Донатын 3 өнгө нь `app.js`-ийн **~185 ба ~1581-р мөрөнд ХАТУУ бичигдсэн**
  (`#3B2FE0` шлам / `#9BA3A9` хаягдал / `#FF9500` бүтээгдэхүүн). CSS хувьсагч
  БИШ тул `:root` солиход өөрчлөгдөхгүй.
- Gauge-ийн gradient нь `gaugeHtml()` доторх SVG `<linearGradient>`-д шууд
  бичигдсэн (app.js ~505-510). CSS-ээр дарах бол
  `.gauge-svg path[stroke^="url"]{stroke:...}`.
- Тээврийн icon: `UI.MAT_ICONS` (app.js ~384) — `transport-icons/*.svg`-ийг
  `<img class="transport-type-icon">`-ээр дууддаг.

## Асуудлын нэгтгэл (2026-08-05-нд нэмсэн)

Захиалагчийн шаардлага: «асуудал хэсэгт асуудлууд хэлтэс хэлтэсээр ялгарч
харагддаг байх, асуудлыг тус бүрийн тайлан нэгтгэх».

- `CONFIG.forms` дотор `ISSUE_FIELDS` гэсэн нийтлэг блок бий бөгөөд
  production / transport / fuel / equipment / camp / hse ЗУРГААН тайлан бүрийн
  төгсгөлд `...ISSUE_FIELDS` гэж spread хийж залгасан. Агуулга нь:
  бүлгийн гарчиг (`type:'sep'`), `issue_text` (textarea), `issue_severity`
  (select: хоосон / low / medium / high). `data_json` чөлөөт JSON тул
  migration хэрэггүй.
- `buildFuelForm` доторх `isTail` шүүлтүүр нь `note` болон `group === 'issue'`
  талбаруудыг машины хүснэгтийн АРД байрлуулна. Шинэ талбар нэмэхдээ
  энэ шүүлтүүрийг санах хэрэгтэй.
- Самбар дээр `issuesCard()` (PageDashboard дотор) бүх модулийн
  `issue_text`-ийг цуглуулж, ноцтой байдлаар (Өндөр→Бага) эрэмбэлж, хэлтэс
  бүрийг өөрийн өнгөт зураас, icon, нэрээр ялган харуулна. Асуудлын модулийн
  бүртгэл «Ерөнхий асуудал» нэрээр төлөв/арга хэмжээ/хариуцагчтайгаа орно.
- `renderSummaryCards()` нь `CONFIG.summaryCards.map(...).join('') + issuesCard()`
  гэж дуусдаг. Асуудлын карт `summaryCards`-д БАЙХГҮЙ — тусад нь үүсдэг.
- Асуудалгүй өдөр ногоон «Асуудалгүй» тэмдэглэгээ + тайлбар мөр гарна.
- Сарын нэгтгэлийн «ХАБЭА ба Асуудал» блок мөн бүх модулийн `issue_text`-ээс
  тоолдог (`monthIssues`), зөвхөн Асуудлын модулиас биш.
- CSS: `.card-issues`, `.issue-list/-item/-top/-dept/-tags/-sev/-st/-text/-meta`,
  `.issue-tally`, `.issue-none`, `.form-sep`, `.detail-issue`.

## Хувилбарын түүх — V20 дизайныг ТАТГАЛЗСАН (чухал)

2026-08-03/04-нд Claude Design-аар шинэ dashboard (V20) хийлгэж үзсэн боловч
захиалагч бүрэн татгалзаж, **claude design-аас өмнөх (2026-08-03-ны өмнөх)
V17 хувилбар руу буцаасан**. Одоогийн бүх ажил тэр V17 суурин дээр явж байна.

- Татгалзсан V20 нь `_v20_design_backup.zip` дотор хадгалагдсан (git-д ордоггүй,
  зөвхөн ажлын компьютер дээр). Хэрэггүй л бол бүү сэргээ.
- **Шинэ дизайн санал болгохгүй.** Захиалагч одоо байгаа дизайныг карт
  тус бүрээр нь жижиглэн сайжруулах горимыг сонгосон.
- Өөрчлөлт бүрийг эхлээд `_preview_dashboard.html` (өөрөө биеэ даасан, CSS/JS/
  зураг бүгд base64-ээр шингэсэн) файлаар үзүүлж батлуулна.
- **«Бүх засвар дууссаны дараа Deploy хийе»** — дунд нь deploy хийхгүй.

## GitHub дээр хадгалах

- Repo: https://github.com/Gobiresource/Report (branch main). Одоогоор public.
- `.\git_save.ps1 "тайлбар"` — git identity тохируулах, түр файлуудыг
  хяналтаас гаргах, `git add -A`, commit, `git push origin main`-ыг цуг хийнэ.
- 2026-08-05: `3c70eec` commit амжилттай хийгдсэн ч push нь **DNS-ийн улмаас**
  бүтэлгүйтсэн — тухайн сүлжээний DNS сервер github.com-ыг «Non-existent
  domain» гэж хариулж, гадны DNS (1.1.1.1) руу хандах порт хаалттай байсан.
  Өөр сүлжээ/компьютерээс `git push origin main` гэвэл гарна.

## Ажлын горим
- Дизайны өөрчлөлтийг эхлээд тайлбарлаж/үзүүлж баталгаажуулаад код руу оруулна.
- Хэрэглэгч файлын карт илгээхийг хүсдэггүй — зөвхөн deploy заавар өгнө:
  `cd <хавтас>; .\deploy.ps1`
- Бүх текст монголоор. Кодын тайлбар монголоор.
